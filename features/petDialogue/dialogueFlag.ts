// LEGACY CAT CODE: inactive; preserved reversibly as JSON-encoded comment lines.
// Active implementation now comes from @mrburdeveloperteam/pet-function/apps/superapp via sharedPet/.
// legacy-line: "// Small typed helper so the raw env check lives in exactly one place instead"
// legacy-line: "// of being scattered across CatMascot/App as inline `import.meta.env` reads."
// legacy-line: "export function isPersonalizedPetDialogueEnabled(): boolean {"
// legacy-line: "  return import.meta.env.VITE_ENABLE_PERSONALIZED_PET_DIALOGUE === 'true';"
// legacy-line: "}"
// legacy-line: ""
