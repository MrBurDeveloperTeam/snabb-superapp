const Showcase = ({ isLoginMode }: { isLoginMode: "login" | "signup" }) => {
  return (
    <div className="hidden md:flex ...">
      {/* EXACT JSX you already have */}
    </div>
  );
};

export default Showcase;
